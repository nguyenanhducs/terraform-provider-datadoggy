---
layout: ""
page_title: "Datadoggo Provider"
description: |-
  Terraform provider for managing Datadog Notebook.
---

# Datadoggo Provider

The official `DataDog/datadog` Terraform provider exposes most Datadog resources but omits the Notebooks. See the upstream request in [DataDog/terraform-provider-datadog issue #261](https://github.com/DataDog/terraform-provider-datadog/issues/261).

This provider implements the `datadoggo_notebook` resource so notebooks can be managed as infrastructure-as-code alongside the rest of your Datadog setup.

## Example Usage

```terraform
terraform {
  required_providers {
    datadoggo = {
      source = "nguyenanhducs/datadoggo"
    }
  }
}

provider "datadoggo" {
  # Configuration options
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) Datadog API key. Can also be set via `DD_API_KEY` environment variable.
- `api_url` (String) Datadog API base URL. Can also be set via `DD_HOST` environment variable. Defaults to `https://api.datadoghq.com`.
- `app_key` (String, Sensitive) Datadog Application key. Can also be set via `DD_APP_KEY` environment variable.
- `validate` (Boolean) Whether to validate credentials on provider initialization. Defaults to `true`.
